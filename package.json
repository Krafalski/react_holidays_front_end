{
  "name": "holidays-front-end",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "env-cmd": "^8.0.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0",
    "skeleton-css": "^2.0.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:heroku": "env-cmd .env.heroku npm run build",
    "build:dev": "env-cmd .env.dev react-scripts start",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "env-cmd .env.heroku react-scripts run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "heroku-run-build-script": true
}
